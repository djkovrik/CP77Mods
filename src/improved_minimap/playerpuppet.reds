import ImprovedMinimapUtil.*

// Methods

// IF YOU READ THIS - THESE ARE A FEW DIRTY HACKS BELOW :(
// Minimap widget reloading with new zoom values can be triggered only by a few events like combat mode, active zone 
// or mount state changing. Fake triggering active zone change felt the safest for me but still is kinda rough solution.

// Overrides
